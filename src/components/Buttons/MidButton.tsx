import React from 'react'
import { Button, ButtonProps } from 'react-native-ui-lib'

interface MidButtonProps {
    onPressFunc: () => void
    props?: ButtonProps
}

export default function MidButton({ onPressFunc, props }: MidButtonProps) {
    return (

    )
}
